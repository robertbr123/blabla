# M1 — Fundação: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Estabelecer a fundação do monorepo Ondeline v2 com `apps/api` (FastAPI) + Postgres + Redis subindo via docker-compose, healthcheck verde, configuração via `.env`, testes pytest passando e CI básico no GitHub Actions.

**Architecture:** Monorepo Python com workspace root + `apps/api` como primeira app. Configuração centralizada via `pydantic-settings` lendo `.env`. Healthchecks `/healthz` (DB+Redis ping) e `/livez` (process alive). Containers em `infra/docker-compose.dev.yml`. CI no GitHub Actions roda lint + tipos + testes.

**Tech Stack:** Python 3.11, FastAPI 0.115+, uvicorn, pydantic-settings 2.x, asyncpg + psycopg, redis-py, pytest + pytest-asyncio + httpx, ruff, mypy, Docker, docker-compose v2, GitHub Actions.

**Pré-requisitos:**
- Repo `github.com/robertbr123/blabla` já existe com 2 commits (`174d750`, `73db722`)
- Working directory: `/root/BLABLA/ondeline-v2/`
- Python 3.11.2 e Docker 29.3.1 já instalados no host
- gh CLI autenticado como `robertbr123`

---

## File Structure (criados neste M1)

```
ondeline-v2/
├── README.md                                  # documentação root
├── .env.example                               # template de secrets
├── Makefile                                   # comandos comuns (dev, test, lint)
├── pyproject.toml                             # workspace Python (root)
├── apps/
│   └── api/
│       ├── pyproject.toml                     # deps da api
│       ├── Dockerfile                         # imagem da api
│       ├── src/
│       │   └── ondeline_api/
│       │       ├── __init__.py                # versão do pacote
│       │       ├── main.py                    # FastAPI app factory
│       │       ├── config.py                  # Settings (pydantic-settings)
│       │       ├── deps.py                    # dependências (DB, Redis pools)
│       │       └── api/
│       │           ├── __init__.py
│       │           └── health.py              # /healthz, /livez
│       └── tests/
│           ├── __init__.py
│           ├── conftest.py                    # fixtures (TestClient)
│           ├── test_config.py                 # testes de Settings
│           └── test_health.py                 # testes /healthz e /livez
├── infra/
│   └── docker-compose.dev.yml                 # postgres + redis + api
└── .github/
    └── workflows/
        └── ci.yml                             # lint + mypy + pytest
```

**Princípio:** cada arquivo tem uma responsabilidade. `config.py` só carrega settings. `health.py` só health endpoints. `deps.py` cria pools de DB/Redis para serem injetados via Depends. `main.py` é a app factory que combina tudo.

---

## Task 1: Workspace root + README + Makefile

**Files:**
- Create: `/root/BLABLA/ondeline-v2/README.md`
- Create: `/root/BLABLA/ondeline-v2/.env.example`
- Create: `/root/BLABLA/ondeline-v2/Makefile`
- Create: `/root/BLABLA/ondeline-v2/pyproject.toml`

- [ ] **Step 1.1: Criar README.md root**

```markdown
# Ondeline v2

Reescrita paralela do bot de WhatsApp + dashboard da Ondeline Telecom.

**Status:** em desenvolvimento (M1 — Fundação).

## Stack

- Backend: FastAPI + Celery (Python 3.11)
- Banco: PostgreSQL 16 (Docker)
- Cache/fila: Redis 7
- Frontend: Next.js 15 (em milestones futuros)

## Comandos rápidos

| Comando | O quê |
|---|---|
| `make dev` | Sobe stack de dev (postgres + redis + api) |
| `make down` | Derruba stack |
| `make test` | Roda pytest |
| `make lint` | Roda ruff + mypy |
| `make logs` | Tail dos logs dos containers |

## Documentação

- Spec de design: [docs/superpowers/specs/2026-05-09-ondeline-v2-fase1-fase2-design.md](docs/superpowers/specs/2026-05-09-ondeline-v2-fase1-fase2-design.md)
- Plano M1: [docs/superpowers/plans/2026-05-09-m1-fundacao.md](docs/superpowers/plans/2026-05-09-m1-fundacao.md)

## Setup local

```bash
cp .env.example .env
# editar .env com seus secrets
make dev
curl http://localhost:8000/healthz
```
```

- [ ] **Step 1.2: Criar .env.example**

```bash
# Aplicação
ENV=development
LOG_LEVEL=INFO

# Banco de dados
DATABASE_URL=postgresql+asyncpg://ondeline:ondeline@postgres:5432/ondeline
DATABASE_URL_SYNC=postgresql+psycopg://ondeline:ondeline@postgres:5432/ondeline

# Redis
REDIS_URL=redis://redis:6379/0

# Evolution API (WhatsApp) — preenchido em milestones futuros
EVOLUTION_URL=http://localhost:8080
EVOLUTION_KEY=
EVOLUTION_HMAC_SECRET=

# SGP (Sistema de Gestão de Provedor) — preenchido em M4
SGP_ONDELINE_BASE=https://ondeline.sgp.tsmx.com.br
SGP_ONDELINE_TOKEN=
SGP_ONDELINE_APP=mikrotik
SGP_LINKNETAM_BASE=https://linknetam.sgp.net.br
SGP_LINKNETAM_TOKEN=
SGP_LINKNETAM_APP=APP

# Hermes LLM — preenchido em M4
HERMES_URL=http://127.0.0.1:8642/v1
HERMES_API_KEY=
HERMES_MODEL=anthropic/claude-opus-4.6

# Auth — preenchido em M2
JWT_SECRET=
PII_ENCRYPTION_KEY=
PII_HASH_PEPPER=

# Observabilidade — opcional
SENTRY_DSN=
```

- [ ] **Step 1.3: Criar Makefile**

```makefile
.PHONY: dev down test lint logs install

dev:
	docker compose -f infra/docker-compose.dev.yml --env-file .env up -d --build

down:
	docker compose -f infra/docker-compose.dev.yml down

logs:
	docker compose -f infra/docker-compose.dev.yml logs -f

install:
	cd apps/api && python3 -m venv .venv && . .venv/bin/activate && pip install -e ".[dev]"

test:
	cd apps/api && . .venv/bin/activate && pytest -v

lint:
	cd apps/api && . .venv/bin/activate && ruff check src tests && mypy src

format:
	cd apps/api && . .venv/bin/activate && ruff format src tests
```

- [ ] **Step 1.4: Criar pyproject.toml root**

```toml
[project]
name = "ondeline-v2"
version = "0.1.0"
description = "Ondeline Telecom v2 monorepo"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "RUF"]
ignore = ["E501"]  # line length handled by formatter

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false
```

- [ ] **Step 1.5: Commit**

```bash
cd /root/BLABLA/ondeline-v2
git add README.md .env.example Makefile pyproject.toml
git commit -m "chore(m1): add workspace root files

- README com stack e comandos
- .env.example com placeholders dos secrets
- Makefile com targets dev/test/lint
- pyproject.toml root com config ruff e mypy"
```

---

## Task 2: apps/api Python package skeleton

**Files:**
- Create: `/root/BLABLA/ondeline-v2/apps/api/pyproject.toml`
- Create: `/root/BLABLA/ondeline-v2/apps/api/src/ondeline_api/__init__.py`
- Create: `/root/BLABLA/ondeline-v2/apps/api/src/ondeline_api/api/__init__.py`
- Create: `/root/BLABLA/ondeline-v2/apps/api/tests/__init__.py`

- [ ] **Step 2.1: Criar apps/api/pyproject.toml**

```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ondeline-api"
version = "0.1.0"
description = "Ondeline v2 API + workers"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
    "asyncpg>=0.30.0",
    "psycopg[binary]>=3.2.0",
    "redis>=5.2.0",
    "structlog>=24.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "httpx>=0.27.0",
    "ruff>=0.7.0",
    "mypy>=1.13.0",
    "types-redis>=4.6.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/ondeline_api"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]
```

- [ ] **Step 2.2: Criar apps/api/src/ondeline_api/__init__.py**

```python
"""Ondeline v2 API package."""

__version__ = "0.1.0"
```

- [ ] **Step 2.3: Criar apps/api/src/ondeline_api/api/__init__.py**

```python
"""HTTP API routers."""
```

- [ ] **Step 2.4: Criar apps/api/tests/__init__.py**

Conteúdo: arquivo vazio (apenas marker do pacote).

```python
```

- [ ] **Step 2.5: Instalar dependências em venv local**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
python3 -m venv .venv
. .venv/bin/activate
pip install -U pip
pip install -e ".[dev]"
```

Expected: `Successfully installed ondeline-api-0.1.0` ao final, sem erros.

- [ ] **Step 2.6: Verificar imports funcionam**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
python -c "import ondeline_api; print(ondeline_api.__version__)"
```

Expected: `0.1.0`

- [ ] **Step 2.7: Commit**

```bash
cd /root/BLABLA/ondeline-v2
git add apps/api/pyproject.toml apps/api/src/ondeline_api/__init__.py \
  apps/api/src/ondeline_api/api/__init__.py apps/api/tests/__init__.py
git commit -m "feat(m1): scaffold apps/api Python package

- pyproject.toml com FastAPI + Pydantic + asyncpg + Redis + structlog
- ondeline_api e ondeline_api.api packages com __init__.py
- venv local instalado via pip install -e .[dev]"
```

---

## Task 3: Config module (TDD)

**Files:**
- Test: `/root/BLABLA/ondeline-v2/apps/api/tests/test_config.py`
- Create: `/root/BLABLA/ondeline-v2/apps/api/src/ondeline_api/config.py`

- [ ] **Step 3.1: Escrever teste falhando (test_config.py)**

```python
"""Tests for the Settings configuration class."""
from __future__ import annotations

import pytest
from pydantic import ValidationError

from ondeline_api.config import Settings


def test_settings_loads_from_env(monkeypatch: pytest.MonkeyPatch) -> None:
    monkeypatch.setenv("ENV", "development")
    monkeypatch.setenv("LOG_LEVEL", "DEBUG")
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql+asyncpg://u:p@localhost:5432/db",
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")

    settings = Settings()

    assert settings.env == "development"
    assert settings.log_level == "DEBUG"
    assert str(settings.database_url) == "postgresql+asyncpg://u:p@localhost:5432/db"
    assert str(settings.redis_url) == "redis://localhost:6379/0"


def test_settings_defaults_when_optional_missing(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    # Required vars set; optional ones absent
    monkeypatch.setenv(
        "DATABASE_URL", "postgresql+asyncpg://u:p@localhost:5432/db"
    )
    monkeypatch.setenv("REDIS_URL", "redis://localhost:6379/0")
    monkeypatch.delenv("EVOLUTION_KEY", raising=False)

    settings = Settings()

    assert settings.env == "development"  # default
    assert settings.evolution_key == ""  # default empty


def test_settings_fails_when_required_missing(
    monkeypatch: pytest.MonkeyPatch,
) -> None:
    monkeypatch.delenv("DATABASE_URL", raising=False)
    monkeypatch.delenv("REDIS_URL", raising=False)

    with pytest.raises(ValidationError):
        Settings()
```

- [ ] **Step 3.2: Rodar teste e verificar que falha**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
pytest tests/test_config.py -v
```

Expected: 3 testes em ERROR/FAIL com `ModuleNotFoundError: No module named 'ondeline_api.config'`.

- [ ] **Step 3.3: Implementar config.py**

```python
"""Application settings loaded from environment variables."""
from __future__ import annotations

from pydantic import Field, RedisDsn
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """Centralized settings. Read once at startup, then immutable."""

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
        case_sensitive=False,
    )

    # Aplicação
    env: str = "development"
    log_level: str = "INFO"

    # Banco
    database_url: str = Field(..., description="postgresql+asyncpg://...")
    database_url_sync: str = Field(
        default="",
        description="psycopg sync URL (Alembic + tools)",
    )

    # Redis
    redis_url: RedisDsn = Field(..., description="redis://host:port/db")

    # Evolution API (preenchido em milestones futuros)
    evolution_url: str = "http://localhost:8080"
    evolution_key: str = ""
    evolution_hmac_secret: str = ""

    # SGP
    sgp_ondeline_base: str = "https://ondeline.sgp.tsmx.com.br"
    sgp_ondeline_token: str = ""
    sgp_ondeline_app: str = "mikrotik"
    sgp_linknetam_base: str = "https://linknetam.sgp.net.br"
    sgp_linknetam_token: str = ""
    sgp_linknetam_app: str = "APP"

    # Hermes LLM
    hermes_url: str = "http://127.0.0.1:8642/v1"
    hermes_api_key: str = ""
    hermes_model: str = "anthropic/claude-opus-4.6"

    # Auth
    jwt_secret: str = ""
    pii_encryption_key: str = ""
    pii_hash_pepper: str = ""

    # Observabilidade
    sentry_dsn: str = ""


def get_settings() -> Settings:
    """Lazy singleton — chamado uma vez via Depends."""
    return Settings()
```

- [ ] **Step 3.4: Rodar testes e verificar que passam**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
pytest tests/test_config.py -v
```

Expected: `3 passed`.

- [ ] **Step 3.5: Verificar tipos com mypy**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
mypy src/ondeline_api/config.py
```

Expected: `Success: no issues found in 1 source file`.

- [ ] **Step 3.6: Commit**

```bash
cd /root/BLABLA/ondeline-v2
git add apps/api/src/ondeline_api/config.py apps/api/tests/test_config.py
git commit -m "feat(m1): add Settings via pydantic-settings

- Settings carrega de .env com defaults seguros
- DATABASE_URL e REDIS_URL obrigatórios
- Outros campos com defaults vazios para preenchimento futuro
- 3 testes (load, defaults, missing required)"
```

---

## Task 4: Health endpoints (TDD)

**Files:**
- Create: `/root/BLABLA/ondeline-v2/apps/api/src/ondeline_api/deps.py`
- Test: `/root/BLABLA/ondeline-v2/apps/api/tests/conftest.py`
- Test: `/root/BLABLA/ondeline-v2/apps/api/tests/test_health.py`
- Create: `/root/BLABLA/ondeline-v2/apps/api/src/ondeline_api/api/health.py`
- Create: `/root/BLABLA/ondeline-v2/apps/api/src/ondeline_api/main.py`

- [ ] **Step 4.1: Criar conftest.py com TestClient e fakes**

```python
"""Pytest fixtures for the API."""
from __future__ import annotations

from collections.abc import AsyncIterator
from typing import Any

import pytest
import pytest_asyncio
from fastapi import FastAPI
from httpx import ASGITransport, AsyncClient


class FakeRedis:
    """Minimal Redis stub for health tests."""

    def __init__(self, *, alive: bool = True) -> None:
        self._alive = alive

    async def ping(self) -> bool:
        if not self._alive:
            raise ConnectionError("redis down")
        return True

    async def aclose(self) -> None:
        return None


class FakeDB:
    """Minimal DB stub for health tests."""

    def __init__(self, *, alive: bool = True) -> None:
        self._alive = alive

    async def fetchval(self, query: str) -> int:
        if not self._alive:
            raise ConnectionError("db down")
        return 1

    async def close(self) -> None:
        return None


@pytest.fixture
def app() -> FastAPI:
    from ondeline_api.main import create_app

    return create_app()


@pytest_asyncio.fixture
async def client(app: FastAPI) -> AsyncIterator[AsyncClient]:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as c:
        yield c


@pytest.fixture
def healthy_deps() -> dict[str, Any]:
    return {"db": FakeDB(alive=True), "redis": FakeRedis(alive=True)}


@pytest.fixture
def broken_db_deps() -> dict[str, Any]:
    return {"db": FakeDB(alive=False), "redis": FakeRedis(alive=True)}
```

- [ ] **Step 4.2: Escrever testes falhando (test_health.py)**

```python
"""Tests for /healthz and /livez endpoints."""
from __future__ import annotations

from typing import Any

import pytest
from fastapi import FastAPI
from httpx import AsyncClient

from ondeline_api.deps import get_db, get_redis


@pytest.mark.asyncio
async def test_livez_returns_200(client: AsyncClient) -> None:
    response = await client.get("/livez")

    assert response.status_code == 200
    assert response.json() == {"status": "alive"}


@pytest.mark.asyncio
async def test_healthz_returns_200_when_all_ok(
    app: FastAPI, client: AsyncClient, healthy_deps: dict[str, Any]
) -> None:
    app.dependency_overrides[get_db] = lambda: healthy_deps["db"]
    app.dependency_overrides[get_redis] = lambda: healthy_deps["redis"]

    response = await client.get("/healthz")

    assert response.status_code == 200
    body = response.json()
    assert body["status"] == "ok"
    assert body["checks"]["db"] == "ok"
    assert body["checks"]["redis"] == "ok"


@pytest.mark.asyncio
async def test_healthz_returns_503_when_db_down(
    app: FastAPI, client: AsyncClient, broken_db_deps: dict[str, Any]
) -> None:
    app.dependency_overrides[get_db] = lambda: broken_db_deps["db"]
    app.dependency_overrides[get_redis] = lambda: broken_db_deps["redis"]

    response = await client.get("/healthz")

    assert response.status_code == 503
    body = response.json()
    assert body["status"] == "degraded"
    assert body["checks"]["db"].startswith("error")
    assert body["checks"]["redis"] == "ok"
```

- [ ] **Step 4.3: Rodar testes e verificar que falham**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
pytest tests/test_health.py -v
```

Expected: ERROR no collection com `ModuleNotFoundError: No module named 'ondeline_api.deps'`.

- [ ] **Step 4.4: Implementar deps.py com pools placeholder**

```python
"""Dependency providers for DB and Redis pools.

Em M1 retornamos pools placeholder. Em M2 trocamos por SQLAlchemy
async engine e redis.asyncio.from_url().
"""
from __future__ import annotations

from collections.abc import AsyncIterator
from functools import lru_cache
from typing import Any, Protocol

import asyncpg
from redis.asyncio import Redis

from ondeline_api.config import Settings, get_settings


class DBLike(Protocol):
    async def fetchval(self, query: str) -> Any: ...


class RedisLike(Protocol):
    async def ping(self) -> bool: ...


@lru_cache(maxsize=1)
def _settings() -> Settings:
    return get_settings()


@lru_cache(maxsize=1)
def _redis_client() -> Redis:
    return Redis.from_url(str(_settings().redis_url), decode_responses=True)


async def _make_db_pool() -> asyncpg.Pool:
    return await asyncpg.create_pool(
        dsn=_settings().database_url.replace("+asyncpg", ""),
        min_size=1,
        max_size=5,
    )


_db_pool: asyncpg.Pool | None = None


async def get_db() -> AsyncIterator[DBLike]:
    """Yield a connection from the pool."""
    global _db_pool
    if _db_pool is None:
        _db_pool = await _make_db_pool()
    async with _db_pool.acquire() as conn:
        yield conn


async def get_redis() -> RedisLike:
    return _redis_client()
```

- [ ] **Step 4.5: Implementar api/health.py**

```python
"""Health and liveness endpoints."""
from __future__ import annotations

from typing import Any

from fastapi import APIRouter, Depends, Response, status

from ondeline_api.deps import DBLike, RedisLike, get_db, get_redis

router = APIRouter(tags=["health"])


@router.get("/livez")
async def livez() -> dict[str, str]:
    """Liveness — process is up."""
    return {"status": "alive"}


@router.get("/healthz")
async def healthz(
    response: Response,
    db: DBLike = Depends(get_db),
    redis: RedisLike = Depends(get_redis),
) -> dict[str, Any]:
    """Readiness — process can serve traffic (DB and Redis reachable)."""
    checks: dict[str, str] = {}

    try:
        await db.fetchval("SELECT 1")
        checks["db"] = "ok"
    except Exception as exc:  # noqa: BLE001
        checks["db"] = f"error: {exc.__class__.__name__}"

    try:
        await redis.ping()
        checks["redis"] = "ok"
    except Exception as exc:  # noqa: BLE001
        checks["redis"] = f"error: {exc.__class__.__name__}"

    all_ok = all(v == "ok" for v in checks.values())
    if not all_ok:
        response.status_code = status.HTTP_503_SERVICE_UNAVAILABLE

    return {
        "status": "ok" if all_ok else "degraded",
        "checks": checks,
    }
```

- [ ] **Step 4.6: Implementar main.py (app factory)**

```python
"""FastAPI application factory."""
from __future__ import annotations

from fastapi import FastAPI

from ondeline_api import __version__
from ondeline_api.api import health


def create_app() -> FastAPI:
    app = FastAPI(
        title="Ondeline API",
        version=__version__,
        description="WhatsApp bot + admin API for Ondeline Telecom",
    )
    app.include_router(health.router)
    return app


app = create_app()
```

- [ ] **Step 4.7: Rodar testes e verificar que passam**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
pytest tests/test_health.py -v
```

Expected: `3 passed`.

- [ ] **Step 4.8: Verificar mypy**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
mypy src
```

Expected: `Success: no issues found in N source files`.

- [ ] **Step 4.9: Verificar ruff**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
ruff check src tests
```

Expected: `All checks passed!`.

- [ ] **Step 4.10: Smoke test local sem Docker**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
. .venv/bin/activate
# /livez não depende de DB, deve responder mesmo sem Postgres
DATABASE_URL=postgresql+asyncpg://x:x@localhost:5432/x \
REDIS_URL=redis://localhost:6379/0 \
uvicorn ondeline_api.main:app --port 8000 &
sleep 1
curl -s http://localhost:8000/livez
echo ""
kill %1 2>/dev/null || true
```

Expected: `{"status":"alive"}`.

- [ ] **Step 4.11: Commit**

```bash
cd /root/BLABLA/ondeline-v2
git add apps/api/src/ondeline_api/main.py \
  apps/api/src/ondeline_api/deps.py \
  apps/api/src/ondeline_api/api/health.py \
  apps/api/tests/conftest.py \
  apps/api/tests/test_health.py
git commit -m "feat(m1): add /healthz and /livez endpoints

- FastAPI app factory em main.py
- deps.py com pools placeholder de DB (asyncpg) e Redis
- /livez retorna 200 sem checar dependencias
- /healthz checa DB+Redis, retorna 503 se algum falhar
- 3 testes com fakes via dependency_overrides"
```

---

## Task 5: Dockerfile da api

**Files:**
- Create: `/root/BLABLA/ondeline-v2/apps/api/Dockerfile`
- Create: `/root/BLABLA/ondeline-v2/apps/api/.dockerignore`

- [ ] **Step 5.1: Criar .dockerignore**

```
.venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
tests
```

- [ ] **Step 5.2: Criar Dockerfile**

```dockerfile
# syntax=docker/dockerfile:1.7

FROM python:3.11-slim-bookworm AS base

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

RUN apt-get update && apt-get install -y --no-install-recommends \
        curl \
    && rm -rf /var/lib/apt/lists/*

# Non-root user
RUN groupadd --system app && useradd --system --gid app --create-home app
WORKDIR /app

# Dependencies first for layer caching
COPY --chown=app:app pyproject.toml /app/
RUN pip install --upgrade pip && \
    pip install fastapi uvicorn[standard] pydantic pydantic-settings asyncpg \
                "psycopg[binary]" redis structlog

COPY --chown=app:app src /app/src
ENV PYTHONPATH=/app/src

USER app

EXPOSE 8000

HEALTHCHECK --interval=10s --timeout=5s --start-period=20s --retries=3 \
    CMD curl -fsS http://localhost:8000/livez || exit 1

CMD ["uvicorn", "ondeline_api.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

- [ ] **Step 5.3: Build da imagem**

```bash
cd /root/BLABLA/ondeline-v2/apps/api
docker build -t ondeline-api:m1 .
```

Expected: `Successfully tagged ondeline-api:m1` ao final, sem erros.

- [ ] **Step 5.4: Smoke test do container isolado (sem Postgres ainda)**

```bash
docker run --rm -d --name ondeline-api-smoke \
  -e DATABASE_URL=postgresql+asyncpg://x:x@nowhere:5432/x \
  -e REDIS_URL=redis://nowhere:6379/0 \
  -p 8001:8000 \
  ondeline-api:m1
sleep 3
curl -s http://localhost:8001/livez
echo ""
docker stop ondeline-api-smoke
```

Expected: `{"status":"alive"}`.

- [ ] **Step 5.5: Commit**

```bash
cd /root/BLABLA/ondeline-v2
git add apps/api/Dockerfile apps/api/.dockerignore
git commit -m "feat(m1): add Dockerfile for api

- python:3.11-slim-bookworm base
- non-root user 'app'
- HEALTHCHECK em /livez
- /livez funciona em container isolado"
```

---

## Task 6: docker-compose.dev.yml com Postgres + Redis + api

**Files:**
- Create: `/root/BLABLA/ondeline-v2/infra/docker-compose.dev.yml`

- [ ] **Step 6.1: Criar docker-compose.dev.yml**

```yaml
name: ondeline-v2

services:
  postgres:
    image: postgres:16-alpine
    container_name: ondeline-postgres
    environment:
      POSTGRES_USER: ondeline
      POSTGRES_PASSWORD: ondeline
      POSTGRES_DB: ondeline
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5433:5432"  # 5433 no host pra não conflitar com o Postgres do Evolution
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "ondeline", "-d", "ondeline"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: ondeline-redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    ports:
      - "127.0.0.1:6380:6379"  # 6380 no host pra não conflitar
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  api:
    build:
      context: ../apps/api
      dockerfile: Dockerfile
    container_name: ondeline-api
    environment:
      ENV: development
      LOG_LEVEL: INFO
      DATABASE_URL: postgresql+asyncpg://ondeline:ondeline@postgres:5432/ondeline
      DATABASE_URL_SYNC: postgresql+psycopg://ondeline:ondeline@postgres:5432/ondeline
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
```

- [ ] **Step 6.2: Criar .env real para o stack subir**

```bash
cd /root/BLABLA/ondeline-v2
cp .env.example .env
# .env já tem defaults dev compatíveis com docker-compose
```

- [ ] **Step 6.3: Subir stack**

```bash
cd /root/BLABLA/ondeline-v2
docker compose -f infra/docker-compose.dev.yml up -d --build
```

Expected: 3 serviços `Started` (postgres, redis, api).

- [ ] **Step 6.4: Aguardar healthcheck e verificar**

```bash
# espera ate api ficar healthy (ou timeout em 60s)
for i in $(seq 1 30); do
  status=$(docker inspect --format='{{.State.Health.Status}}' ondeline-api 2>/dev/null || echo "starting")
  echo "[$i/30] api status: $status"
  [ "$status" = "healthy" ] && break
  sleep 2
done
docker compose -f infra/docker-compose.dev.yml ps
```

Expected: `ondeline-api` com status `Up X seconds (healthy)`.

- [ ] **Step 6.5: Validar /healthz com tudo conectado**

```bash
curl -sv http://localhost:8000/healthz | head -20
echo ""
curl -s http://localhost:8000/livez
```

Expected:
- `/healthz` retorna 200 com `{"status":"ok","checks":{"db":"ok","redis":"ok"}}`
- `/livez` retorna `{"status":"alive"}`

- [ ] **Step 6.6: Validar resilience — derrubar Postgres e checar 503**

```bash
docker stop ondeline-postgres
sleep 2
curl -sv http://localhost:8000/healthz | head -20
docker start ondeline-postgres
```

Expected: status 503 com `"db":"error: ..."`. Após restart, `/healthz` volta a 200.

- [ ] **Step 6.7: Derrubar tudo**

```bash
cd /root/BLABLA/ondeline-v2
docker compose -f infra/docker-compose.dev.yml down
```

- [ ] **Step 6.8: Commit**

```bash
cd /root/BLABLA/ondeline-v2
git add infra/docker-compose.dev.yml
git commit -m "feat(m1): add docker-compose.dev.yml

- Postgres 16 em 5433 (host) para não conflitar com Evolution
- Redis 7 em 6380 (host)
- api espera postgres+redis healthy via depends_on
- /healthz green com toda a stack up
- /healthz retorna 503 quando Postgres está down (validado)"
```

---

## Task 7: GitHub Actions CI

**Files:**
- Create: `/root/BLABLA/ondeline-v2/.github/workflows/ci.yml`

- [ ] **Step 7.1: Criar .github/workflows/ci.yml**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  api:
    name: api (lint + types + tests)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ondeline
          POSTGRES_PASSWORD: ondeline
          POSTGRES_DB: ondeline
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ondeline"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: apps/api/pyproject.toml

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Ruff
        run: ruff check src tests

      - name: Mypy
        run: mypy src

      - name: Pytest
        env:
          DATABASE_URL: postgresql+asyncpg://ondeline:ondeline@localhost:5432/ondeline
          REDIS_URL: redis://localhost:6379/0
        run: pytest -v
```

- [ ] **Step 7.2: Commit e push**

```bash
cd /root/BLABLA/ondeline-v2
git add .github/workflows/ci.yml
git commit -m "ci(m1): add GitHub Actions workflow

- Roda em push/PR para main
- Job 'api': ruff + mypy --strict + pytest
- Services Postgres 16 e Redis 7 para testes
- Cache de pip via setup-python"
git push
```

- [ ] **Step 7.3: Verificar workflow rodando no GitHub**

```bash
sleep 5
gh run list --limit 3
gh run watch
```

Expected: Workflow `CI` rodando. Aguardar finalização e ver `completed success`.

Se falhar, ler o output:
```bash
gh run view --log-failed
```

E corrigir conforme erro. Re-commit + push até passar.

- [ ] **Step 7.4: Adicionar badge ao README**

Editar `/root/BLABLA/ondeline-v2/README.md` para incluir, logo abaixo do título:

```markdown
[![CI](https://github.com/robertbr123/blabla/actions/workflows/ci.yml/badge.svg)](https://github.com/robertbr123/blabla/actions/workflows/ci.yml)
```

- [ ] **Step 7.5: Commit final do M1**

```bash
cd /root/BLABLA/ondeline-v2
git add README.md
git commit -m "docs(m1): add CI badge to README"
git push
```

---

## Task 8: Smoke test final e tag M1

**Files:** N/A (apenas verificação)

- [ ] **Step 8.1: Stack sobe limpo**

```bash
cd /root/BLABLA/ondeline-v2
docker compose -f infra/docker-compose.dev.yml down -v  # zera volumes
make dev
```

Expected: 3 serviços healthy em até 30s.

- [ ] **Step 8.2: Healthcheck verde**

```bash
sleep 15
curl -s http://localhost:8000/healthz | python3 -m json.tool
curl -s http://localhost:8000/livez | python3 -m json.tool
```

Expected:
```json
{"status": "ok", "checks": {"db": "ok", "redis": "ok"}}
{"status": "alive"}
```

- [ ] **Step 8.3: Testes passando localmente**

```bash
cd /root/BLABLA/ondeline-v2
make test
```

Expected: `6 passed` (3 config + 3 health).

- [ ] **Step 8.4: Lint limpo**

```bash
cd /root/BLABLA/ondeline-v2
make lint
```

Expected: `All checks passed!` + `Success: no issues found`.

- [ ] **Step 8.5: CI verde no GitHub**

```bash
gh run list --workflow ci.yml --limit 1
```

Expected: status `completed success`.

- [ ] **Step 8.6: Tag M1 e push**

```bash
cd /root/BLABLA/ondeline-v2
git tag -a m1-fundacao -m "M1: Fundação concluída

- Monorepo Python skeleton
- apps/api com FastAPI + healthchecks
- Postgres 16 + Redis 7 via docker-compose
- /healthz e /livez funcionando
- Testes pytest passando (6/6)
- CI GitHub Actions verde
- Stack sobe via 'make dev'"
git push --tags
```

- [ ] **Step 8.7: Derrubar stack (cleanup)**

```bash
cd /root/BLABLA/ondeline-v2
make down
```

---

## Definition of Done — M1

- [x] Repo no GitHub com workflows configurados
- [x] `make dev` sobe Postgres + Redis + api em containers
- [x] `curl http://localhost:8000/healthz` retorna 200 com checks ok
- [x] `curl http://localhost:8000/livez` retorna 200
- [x] `make test` passa (6/6 testes verdes)
- [x] `make lint` passa (ruff + mypy --strict)
- [x] CI verde no `main` no GitHub Actions
- [x] Tag `m1-fundacao` criada e pushada
- [x] Stack derruba limpo via `make down`

## Próximos passos (não fazem parte do M1)

- M2 (Banco + Auth): schema completo, migrações Alembic, auth JWT, RBAC, audit_log, criptografia PII
- M3 (Bot core): webhook HMAC + Celery + FSM + persistência

## Open questions resolvidas durante M1

(Caso surjam, documentar aqui antes de tagear M1.)

## Notas operacionais

- Postgres do v2 escuta na porta **5433** do host para não conflitar com o Postgres do Evolution API (que escuta na 5432). Dentro da rede Docker `ondeline-v2`, postgres responde em `postgres:5432` normalmente.
- Redis do v2 escuta na **6380** do host pelo mesmo motivo.
- Em CI o conflito não existe: services do GitHub Actions não compartilham porta com nada.
