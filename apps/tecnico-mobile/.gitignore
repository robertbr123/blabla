# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
pubspec.lock
build/
.idea/
.vscode/
*.iml

# Generated code (drift)
lib/**/*.g.dart

# Native (gerados via `flutter create .`)
android/
ios/
linux/
macos/
windows/
web/

# Mas TRACK estes arquivos customizados (app id, permissoes, label, etc).
# Negacao precisa vir DEPOIS do ignore. Demais arquivos sao add -f no commit.
!android/app/build.gradle.kts
!android/settings.gradle.kts
!android/app/src/main/AndroidManifest.xml
!ios/Runner/Info.plist

# Firebase
google-services.json
GoogleService-Info.plist
firebase_options.dart
