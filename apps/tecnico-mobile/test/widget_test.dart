// Substitui o widget_test default gerado pelo `flutter create` — que
// referencia MyApp inexistente. Mantemos um placeholder valido pra CI.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
